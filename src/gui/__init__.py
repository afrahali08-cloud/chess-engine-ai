"""Windowed frontend.

Intentionally empty: the pure modules in this package (layout, interaction,
engine_worker, viewmodel) must stay importable without pygame installed, and an
eager import here would break that.
"""
